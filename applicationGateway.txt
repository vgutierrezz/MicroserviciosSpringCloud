server:
  port: 8083
spring:
  application:
    name: gateway-service
  config:
    import: "optional:configserver:"
  cloud:
    config:
        enabled: true
        discovery:
          enabled: false
          serviceId: gateway
    gateway:
      routes:
        - id: checkout-service
          uri: lb://checkout-service
          predicates:
            - Path=/checkout
            - Path=/checkout/**
          filters:
            - AddRequestHeader=X-Request-from,gateway
            - AddResponseHeader=X-Response-Red, Blue
        - id: product-service
          uri: lb://product-service
          predicates:
            - Path=/product
            - Path=/product/**
          filters:
            - AddRequestHeader=X-Request-from,gateway
            - AddResponseHeader=X-Response-Red, Blue

eureka:
  instance:
    hostname: localhost
    statusPageUrlPath: /actuator/info
    healthCheckUrlPath: /actuator/health
    non-secure-port: 8083
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway,routes
  info:
    env:
      enabled: true

info:
  app:
    name: GATEWAY Service
    description: Servicio para la redirección de solicitudes a los microservicios correspondientes.
    version: 1.0.0
